#include "main.h"

/**
 *mul - multiplies two numbers
 *@a: first argument/number to be multiplied
 *@b: second argument/number to be multplied
 *Return: returns product of the numbers
 */

int mul(int a, int b)
{
return (a * b);
}
