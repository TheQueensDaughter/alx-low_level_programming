#include "main.h"

/**
 *add- adds two numbers and prints the sum
 *@a: first argument
 *@b: second argument
 *Return: The sum of the numbers
 */

int add(int a, int b)
{
int sum = a + b;
return (sum);
}
