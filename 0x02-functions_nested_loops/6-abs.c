#include "main.h"

/**
 *_abs- function that prints absolut value of a number
 *@c: the variable that is accessed as the argument
 *Return:c(positive) -c(negative)
 */

int _abs(int c)
{
if (c >= 0)
{
return (c);
}
else
{
return (c * -1);
}
}
